# Cloudflare Worker base URL used by the admin UI for all API requests.
# Replace with your deployed LINE CRM worker URL.
NEXT_PUBLIC_API_URL=https://line-crm-worker.<your-subdomain>.workers.dev

# Optional UI version label shown in the sidebar footer.
APP_VERSION=0.1.0-local
